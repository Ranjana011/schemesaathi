{
  "name": "scheme-saathi-auth-server",
  "version": "1.0.0",
  "description": "Node/Express + MongoDB authentication backend for SchemeSaathi (signup, login, account, JWT sessions). Runs alongside the existing Flask ML backend on a different port.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.6.0"
  }
}
